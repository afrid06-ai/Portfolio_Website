# Afrid — AI/ML Engineer Portfolio

A responsive, single-page portfolio that presents AI engineering experience as an interactive visual narrative. It is built with semantic HTML, modern CSS, and dependency-free JavaScript.

## Run locally

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
